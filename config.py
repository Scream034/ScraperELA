"""
Универсальный конфигурационный файл ScraperELA (ScraperElena).
"""

# =====================================================================
# РЕЖИМЫ РАБОТЫ (УПРАВЛЕНИЕ ЗАПУСКОМ)
# =====================================================================
RUN_CATALOG_SCAN = True
RUN_DETAIL_PARSER = True
RUN_EXPORTER = True

# Активный сайт из реестра SITES
ACTIVE_SITE = "chop_moscow"


# =====================================================================
# РЕЕСТР ПОДДЕРЖИВАЕМЫХ САЙТОВ (SITES REGISTRY)
# =====================================================================
SITES = {
    "chop_moscow": {
        "name": "Реестр ЧОП Москвы (chop.moscow)",
        "base_url": "https://chop.moscow/catalog/",
        "db_name": "chop_database.db",
        "export_name": "chops_export",
        "parser_key": "chop_moscow",
        # 0 означает "парсить до упора"
        "max_pages": 0,
        # ЛИМИТЫ ПОТОКОВ (КОНКУРЕНТНОСТЬ)
        "catalog_concurrency": 3,
        "detail_concurrency": 6,
    }
}


# =====================================================================
# КЭШИРОВАНИЕ HTML-СТРАНИЦ (SPEED-UP & DEV MODE)
# =====================================================================
# Сохранять скачанные HTML-страницы локально на диск?
# True  - Карточки качаются из сети только 1 раз. При повторном запуске скрипт мгновенно
#         читает их с SSD. Рекомендуется для отладки и стабильности.
# False - Всегда качать свежие страницы из интернета.
USE_HTML_CACHE = True

# Срок жизни кэша в днях (TTL - Time To Live).
# По истечении этого срока файл кэша на диске будет автоматически проигнорирован,
# а страница скачается заново для обновления данных.
# Поставьте 0, чтобы сделать кэш бесконечным (обновление только вручную).
CACHE_TTL_DAYS = 30

# Сохранять историю изменений страниц?
# True  - При обновлении устаревшего кэша старый HTML-файл не затирается,
#         а переносится в папку "history" с меткой даты. Позволяет тестировать
#         код на старой верстке и отслеживать историю изменений (адресов, телефонов).
# False - Старый кэш перезаписывается новым (экономит место на диске).
KEEP_CACHE_HISTORY = True

# =====================================================================
# АДАПТИВНОЕ РЕГУЛИРОВАНИЕ СКОРОСТИ (SMART THROTTLING)
# =====================================================================
ADAPTIVE_MODE = True
ADAPTIVE_MAX_DELAY = 10.0


# =====================================================================
# СЕТЕВЫЕ НАСТРОЙКИ ОТКАЗОУСТОЙЧИВОСТИ
# =====================================================================
REQUEST_DELAY = 0.5
NETWORK_RETRIES = 5
BACKOFF_FACTOR = 1.0

EXPORT_TO_XLSX = True
EXPORT_TO_CSV = True
